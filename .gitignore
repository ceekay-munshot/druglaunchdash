node_modules
dist
dist-ssr
*.local
.vite
.DS_Store
.idea
.vscode
*.log

# Subscription-only research PDFs (e.g. IIFL Capital Services). Their data
# is extracted into src/data/ipmReport.js; the source PDFs themselves
# should not live in the repo.
*.pdf

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
